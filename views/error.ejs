<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Error | FileSensei</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-light d-flex flex-column justify-content-center align-items-center vh-100 text-center px-3"
  >
    <!-- Mascot -->
    <img src="../img/senseiError.png" alt="Mascot" class="w-32 mb-4" />

    <!-- Error Code -->
    <h1 class="display-3 text-danger fw-bold mb-3">
      Error <%= typeof status !== 'undefined' ? status : 500 %>
    </h1>

    <!-- Error Message Box -->
    <div
      class="bg-white shadow-sm p-3 rounded text-start max-w-2xl w-full overflow-auto"
    >
      <pre class="whitespace-pre-wrap break-words text-danger-emphasis text-sm">
<%= typeof message !== 'undefined' ? message : 'An unexpected error occurred' %></pre
      >
    </div>

    <!-- Buttons -->
    <div class="d-flex gap-2 justify-content-center mt-4">
      <a
        href="/dashboard"
        class="btn btn-primary shadow-sm px-4 py-2 rounded-pill"
        >Return to Dashboard</a
      >
      <a href="/" class="btn btn-outline-secondary px-4 py-2 rounded-pill"
        >Home</a
      >
    </div>

    <!-- Footer -->
    <footer class="mt-5 text-muted small">
      <p>Need help? Contact developer or try refreshing the page.</p>
    </footer>
  </body>
</html>
